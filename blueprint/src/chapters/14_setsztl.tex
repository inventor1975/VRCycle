\chapter{Operational sets under ZTL verdicts (VR Part II)}
\label{chap:setsztl}
\uses{chap:setsop}

\section{Position}
\label{sec:setsztl_position}

This chapter opens \textbf{VR Part II}: raising VR onto ZTL --- Zero-Trust
Logic, the curator's three-symbol logic of ``truth is never granted on
credit'' (Zenodo, DOI \texttt{10.5281/zenodo.21318982}; the measured Python
twin of this chapter is \texttt{zopsets.py}, expedition E21, in the ZTL
repository).  Step~(а) of the programme: the \emph{witnessed identity} of the
operational set universe (Chapter~\ref{chap:setsop}) \emph{is} a ZTL atom
discipline.  An identity atom $x \approx y$ earns \textsf{T} by carrying a
bisimulation, earns \textsf{F} by carrying a refutation (apartness at a finite
separation stage), and is worth the input mark \textsf{Z} until examined.
Nothing is translated between the two sides --- the disciplines coincide;
the Lean development makes the coincidence a set of kernel-checked theorems.

The ZTL value kernel is \emph{vendored} into
\texttt{VRCycle/SetsZTL/Kernel.lean} with a commit pin (the dependency is
one-way: ZTL never depends on VR).  The kernel stands on the \emph{empty}
axiom list, as it does at home; the whole verdict layer of this chapter is
likewise \axiomprofile{[]}; after the 2026-07-12 tier pass of
Chapter~\ref{chap:setsop} (Iff-\texttt{rw} repaid with combinators) even the
\texttt{vn\_inj} route is axiom-free, so the ENTIRE module stands on the
empty axiom list.

\begin{definition}[The verdict carried with its certificate]
\label{def:setsztl_eqverdict}
\lean{VRCycle.SetsZTL.EqVerdict, VRCycle.SetsZTL.EqVerdict.val}
\leanok
\uses{def:setsop_opset}
\texttt{EqVerdict x y} has three constructors: \texttt{earnedT} holding a
bisimulation witness $x \approx y$, \texttt{earnedF} holding a refutation,
and \texttt{unexamined}; \texttt{val} sends them to \textsf{T}, \textsf{F},
\textsf{Z}.  Soundness is not a theorem about the type --- it is its shape:
\texttt{sound\_T}/\texttt{sound\_F} extract the certificate from the value
(\axiomprofile{[]}).  The zero-trust start is \texttt{unexamined}
(\texttt{zero\_trust\_start}: its value is \textsf{Z}).
\end{definition}

\begin{theorem}[The alive rules are witness constructors]
\label{thm:setsztl_constructors}
\lean{VRCycle.SetsZTL.EqVerdict.refl_val, VRCycle.SetsZTL.EqVerdict.symm_val, VRCycle.SetsZTL.EqVerdict.composeT_val}
\leanok
\uses{def:setsztl_eqverdict, thm:setsop_equiv}
\texttt{refl} (the diagonal), \texttt{symm} (the converse, verdict-preserving)
and \texttt{composeT} (composition of carried witnesses via
\texttt{Equiv.trans}: two earned identities earn the third) --- each
\axiomprofile{[]}.  The ZTL inference rules alive on $\approx$-atoms are not
table luck; each is backed by a certificate constructor.
\end{theorem}

\begin{theorem}[The vn register is total and classical]
\label{thm:setsztl_vn}
\lean{VRCycle.SetsZTL.vnVerdict_ne_Z, VRCycle.SetsZTL.vnVerdict_T_iff}
\leanok
\uses{def:setsztl_eqverdict}
On the operational naturals every identity atom decides:
\texttt{vnVerdict n m} is never \textsf{Z}, and its value is \textsf{T}
exactly when $n = m$ (\textsf{F} earned through \texttt{vn\_inj}).  Identity
on this register is \emph{totally earnable} --- the Lean twin of E21~\S1.
Both \axiomprofile{[]}.
\end{theorem}

\begin{theorem}[Groundedness is orthogonal to earnability]
\label{thm:setsztl_orthogonality}
\lean{VRCycle.SetsZTL.quine_orthogonality, VRCycle.SetsZTL.quineVsEmpty_val}
\leanok
\uses{def:setsztl_eqverdict}
The Quine atom $\Omega = \{\Omega\}$ earns its identity
(\texttt{refl} gives \textsf{T}) while $\neg\mathrm{IsGrounded}\,\Omega$;
and $\Omega$ earns \textsf{F} against $\varnothing$ --- apartness across the
grounded/ungrounded divide.  The cycle that dooms a sentence (the liar:
permanent quarantine, an lfp leftover) is harmless in a set (identity is
gfp-shaped): AFA-as-theorem is the set-side mirror of
quarantine-as-theorem.  All \axiomprofile{[]}.
\end{theorem}

\begin{theorem}[The fully-earned register is classical]
\label{thm:setsztl_classical}
\lean{VRCycle.SetsZTL.earned_register_classical, VRCycle.SetsZTL.earned_register_ne_Z, VRCycle.SetsZTL.earned_register_T_iff}
\leanok
\uses{def:setsztl_eqverdict}
If every atom of a formula carries a decided, truthful verdict, then the
greedy ZTL value of \emph{every} formula over the six connectives is decided
and truthful: \textsf{T} exactly on the classically true formulas, \textsf{Z}
nowhere.  Over verified identity atoms the zero-trust connectives neither lie
nor refuse.  Proved by one structural induction with a per-connective
\texttt{Decided} calculus; \axiomprofile{[]} (empty axiom list --- no
\texttt{propext}).
\end{theorem}

\section{Choice sequences as the lazy register (step (б))}
\label{sec:setsztl_stages}

The measured twin is \texttt{zchoice.py} (E22).  The objects are VR's own:
a \texttt{Branch} of the binary spread (Chapter~\ref{chap:continuum}) is the
sequence-in-growth, its performed segment \texttt{take} is the LAZY register,
and the stage court \texttt{Forces} --- what the segment forces over every
continuation --- is where the greedy register's verdicts are judged.  The
Branch layer inherits the Continuum's own choice-free tier
(\axiomprofile{[propext(, Quot.sound)]}); the kernel cells are
\axiomprofile{[]}.

\begin{definition}[The stage court]
\label{def:setsztl_forces}
\lean{VRCycle.SetsZTL.Forces}
\leanok
Node $s$ \textbf{forces} $P$ when every branch through $s$ satisfies $P$.
\axiomprofile{[]}.
\end{definition}

\begin{theorem}[Heredity is native to the lazy register]
\label{thm:setsztl_heredity}
\lean{VRCycle.SetsZTL.through_mono, VRCycle.SetsZTL.forces_mono}
\leanok
\uses{def:setsztl_forces}
A verdict forced at a node is forced at every extension: earned-at-a-stage
survives every future choice (E22 §3: 0 revocations vs 1371 greedy deaths).
The Kripke persistence the greedy register refuses as a free axiom (E12) is
native to the lazy one.
\end{theorem}

\begin{theorem}[The ladder cells: p→p redeemed, ¬¬ overclaims]
\label{thm:setsztl_ladder}
\lean{VRCycle.SetsZTL.identity_forced_at_root, VRCycle.SetsZTL.identity_greedy_abstains, VRCycle.SetsZTL.ladder_overclaims, VRCycle.SetsZTL.atom_not_forced_at_root}
\leanok
\uses{def:setsztl_forces}
The identity law, fallen in the greedy court
(\texttt{zimp Z Z = F}), is \emph{redeemed} by the stage court (every branch
satisfies it) --- a law of logic, not of data; while the ¬¬ ladder verdict
(\texttt{znot (znot Z) = T}) asserts an atom NO prefix forces (the all-false
branch refutes it).  Raw greedy verdicts are not assertions about the future;
warranted ones are (E22 §1: 0 violations for the sound grade).  All four
cells \axiomprofile{[]}.
\end{theorem}

\begin{theorem}[Lawless stage = global supervaluation]
\label{thm:setsztl_super}
\lean{VRCycle.SetsZTL.stage_eq_super, VRCycle.SetsZTL.pad_through}
\leanok
\uses{def:setsztl_forces, thm:setsztl_heredity}
For a property read off the horizon-$H$ segment, forcing at node $s$ is
EXACTLY the universal quantifier over all finite completions of $s$ to
length $H$ --- the world-set of the global $\square$ (E22 §2: measured
totally, 49545 pairs, then kernel-checked here).  Zero trust is
assertability against a maximally ignorant future; a law narrows the worlds
and strengthens the stage.
\end{theorem}

\begin{theorem}[Stream identity at a stage]
\label{thm:setsztl_streams}
\lean{VRCycle.SetsZTL.eq_never_forced, VRCycle.SetsZTL.apart_earned, VRCycle.SetsZTL.through_pointwise}
\leanok
\uses{def:setsztl_forces}
Equality of branches is never forced at a finite stage (below any node two
explicit continuations diverge), while apartness is earned by one
disagreeing reveal and, being an $\exists$-fact, persists.  E6 recast
per-stage: stream equality is Z-permanent, apartness earnable ---
constructive practice reproduced by the registers.
\end{theorem}

\section{The survival ledger (step (в))}
\label{sec:setsztl_survival}

Criterion C2 (curator, 2026-07-12): \textbf{a proof survives the move onto
ZTL iff it stands below the classical floor} — ZTL's fallen laws are the laws
of free truth, their Lean fingerprint is \texttt{Classical.choice}, and a
constructive proof from earned premises is a chain of ZTL-alive rules
(measured: E20 rules 14/14; E21 §3 constructors; E22 §1 warranty). The
four-tier axiom ledger the cycle has kept from its first work is therefore
the ZTL-survival audit — ``VR always stood on ZTL,'' literally.  The full
MEASURED ledger is \texttt{ZTL\_SURVIVAL.md} (405 live-audited objects +
flagship anchors): everything VR calls operational moves; what stays is
exactly what was already flagged classical by design, by substrate, or by
borrowed plumbing.  The audit bounds proofs, never theorems (auditor, not
separator).

\begin{theorem}[Spot-checks: three flagships, moved]
\label{thm:setsztl_survival}
\lean{VRCycle.SetsZTL.extVerdict_val, VRCycle.SetsZTL.afaVerdict_val, VRCycle.SetsZTL.vnMemVerdict_T_iff, VRCycle.SetsZTL.memCongrVerdict_val}
\leanok
\uses{def:setsztl_eqverdict, thm:setsop_ext}
Strong extensionality moves as a certificate constructor
(\texttt{extVerdict}: from member-matching data the identity witness is
built); AFA-as-theorem moves (\texttt{afaVerdict}: decorations carry earned
identity at every vertex); membership becomes a carried verdict
(\texttt{MemVerdict} — the membership twin of \texttt{EqVerdict}, closing
step~(а)'s gap: the vn register total and classical, earned $\in$
transported along earned $\approx$ by \texttt{memCongrVerdict}).  All on the
empty axiom list.
\end{theorem}

What is \emph{deliberately not} formalised here: the warranty ladder over
partially examined registers (sound~$\supset$~hereditary, the E21 find that
revised ZTL's E12) lives and is measured in the ZTL repository; its Lean twin
belongs to the ZTL corpus, not to VR.
